{
  "framework": null,
  "installCommand": "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git .flutter && .flutter/bin/flutter pub get",
  "buildCommand": ".flutter/bin/flutter build web --release",
  "outputDirectory": "build/web"
}
